FROM node:slim

WORKDIR /app

COPY package.json ./
RUN npm install

COPY . ./

ENV TIMEOUT
ENV MIN_WIDTH
ENV MAX_WIDTH
ENV MIN_HEIGHT
ENV MAX_HEIGHT
ENV MIN_INTERVAL
ENV MAX_INTERVAL
ENV MIN_COUNT
ENV MAX_COUNT
EXPOSE 3000

ENTRYPOINT node src/index.js
